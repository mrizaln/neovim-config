-- the plugins setup needs to be in the following order:
--  1. mason.nvim
--  2. mason-lspconfig.nvim
--  3. setup servers via lspconfig

-- the other two is in a dedicated file: [~/.config/nvim/lua/lsp_setup.lua]
